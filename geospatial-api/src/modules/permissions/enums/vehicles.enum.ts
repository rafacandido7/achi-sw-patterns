export enum VehiclesPermission {
  all = 'vehicles.all',
  find = 'vehicles.find',
  findAll = 'vehicles.findAll',
  create = 'vehicles.create',
  update = 'vehicles.update',
  delete = 'vehicles.delete',
}
