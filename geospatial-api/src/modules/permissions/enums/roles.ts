import { GroupsPermission } from './groups.enum'
import { UsersPermission } from './users.enum'
import { VehiclesPermission } from './vehicles.enum'

export const Role = {
  groups: GroupsPermission,
  users: UsersPermission,
  vehicles: VehiclesPermission,
}
